sudo docker compose exec -T db mysql -u root -pEYTSHpRVbOYu3G9evtDT zoo2_prod < /home/melanie/zoo2-specialcoats.sql

SET FOREIGN_KEY_CHECKS = 0;
RENAME TABLE `specialCoatText` TO `specialCoatsText`;
SET FOREIGN_KEY_CHECKS = 1;
EXIT;

Kontext & Ziel:
Ich strukturiere gerade meine Internationalisierung in meinem Next.js-Projekt (next-intl / i18next) um. Ich habe eine große, vollständige de.json im Hauptverzeichnis. Diese möchte ich nun sauber auflösen und die Inhalte in spezifische Namespaces aufteilen.

Lege im Ordner messages/de/ Dateien für die jeweiligen Kategorien an.

Die Ziel-Dateien im Ordner messages/de/:

animal.json (Alles rund um Tiere)

biome.json (Alles rund um Gehege/Biome)

specialCoat.json (Alles rund um Farbvarianten)

contest.json (Alles rund um Wettbewerbe)

user.json / navigation.json / common.json / page.json ... (entsprechend der restlichen Struktur)

Unser Vorgehen (Schritt für Schritt):

Schritt 1 (Die Aufteilung): Ich poste dir gleich den kompletten Inhalt der großen de.json. Bitte teile alle darin enthaltenen Keys logisch auf die oben genannten Dateien auf und generiere mir den fertigen JSON-Inhalt für jede einzelne Datei, damit ich ihn hineinkopieren kann.

Schritt 2 (Code-Refactoring): Sobald alle JSON-Dateien befüllt sind, gehen wir Schritt für Schritt meine Komponenten und Pages durch. Du hilfst mir dabei, die useTranslations-Hooks auf die neuen Namespaces anzupassen (z.B. useTranslations('animal') statt dem alten globalen Aufruf).

Wenn du bereit bist, sag kurz Bescheid, dann schicke ich dir im nächsten Post den Inhalt der großen de.json!